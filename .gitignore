# OS
.DS_Store

# Terraform
.terraform/
*.tfstate
*.tfstate.backup
tfplan*
*.tfplan
.terraform.tfstate.lock.info
# Mantém o .terraform.lock.hcl (provider lock) versionado.

# Segredos reais
terraform.tfvars
